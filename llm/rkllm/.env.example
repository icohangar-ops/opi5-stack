# RKLLM OpenAI-shim environment variables.
# Copy this file to `.env` (which is gitignored) and set a real token.
# The shim is fail-closed: if RKLLM_API_KEY is unset/empty, every request is
# rejected. Use the same value as the API key configured in Home Assistant.

RKLLM_API_KEY=changeme
